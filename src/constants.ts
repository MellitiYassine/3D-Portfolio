import * as THREE from 'three';

export const CAMERA_DISTANCE = 20;
export const CAMERA_ALTITUDE = 20;
export const CAMERA_SPEED = 0.36;
export const AXIS_Y = new THREE.Vector3(0, 1, 0);
export const LOOK_AT_STEP = 0.001;

export const ZOOM_SPEED: number = 0.3;
export const MIN_FOV: number = 10;
export const MAX_FOV: number = 50;
export const CAMERA_MOUSE_SPEED = 50
